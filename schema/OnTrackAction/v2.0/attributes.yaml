openapi: 3.1.1
info:
  title: OnTrackAction
  version: 2.0.0
  description: 'Beckn /beckn/on_track callback envelope. Sent by a BPP to a BAP in
    response to a /beckn/track call, returning a Tracking handle with the URL and/or
    WebSocket endpoint for real-time fulfillment tracking.

    '
  license:
    name: CC-BY-NC-SA 4.0 International
  contact:
    name: Beckn Labs
    url: https://beckn.io
paths: {}
components:
  schemas:
    OnTrackAction:
      title: On Track Action
      description: 'Beckn /beckn/on_track message payload. Sent by a BPP to a BAP
        in

        response to a /beckn/track call, returning a Tracking handle with

        the URL and/or WebSocket endpoint for real-time fulfillment tracking.

        (Context wrapper stripped; only the message-content portion is inlined.)'
      type: object
      additionalProperties: false
      properties:
        tracking:
          $ref: https://schema.beckn.io/Tracking/v2.1/attributes.yaml#/components/schemas/Tracking
      required:
      - tracking
