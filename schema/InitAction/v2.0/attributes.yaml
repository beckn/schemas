openapi: 3.1.1
info:
  title: InitAction
  version: 2.0.0
  description: 'Beckn /beckn/init action envelope. Sent by a BAP to a BPP to initialise
    a contract with consumer details (billing address, fulfillment preferences, etc.).

    '
  license:
    name: CC-BY-NC-SA 4.0 International
  contact:
    name: Beckn Labs
    url: https://beckn.io
paths: {}
components:
  schemas:
    InitAction:
      title: Init Action
      description: 'Beckn /beckn/init message payload. Sent by a BAP to a BPP to initialise

        a contract with consumer details (billing address, fulfillment preferences,
        etc.).

        (Context wrapper stripped; only the message-content portion is inlined.)'
      additionalProperties: false
      properties:
        contract:
          $ref: https://schema.beckn.io/Contract/v2.0/attributes.yaml#/components/schemas/Contract
      required:
      - contract
