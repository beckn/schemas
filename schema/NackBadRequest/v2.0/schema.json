{
  "description": "NACK — Bad Request: Malformed or invalid request; the server could not parse or validate the payload.",
  "allOf": [
    {
      "$ref": "https://schema.beckn.io/Ack/v2.0/schema.json#/components/schemas/Ack"
    },
    {
      "type": "object",
      "properties": {
        "status": {
          "type": "string",
          "const": "NACK"
        },
        "error": {
          "$ref": "https://schema.beckn.io/Error/v2.0/schema.json#/components/schemas/Error"
        }
      }
    }
  ]
}
