openapi: 3.1.1
info:
  title: OnRateAction
  version: 2.0.0
  description: 'Beckn /beckn/on_rate callback envelope. Sent by a BPP to a BAP in
    response to a /beckn/rate call, optionally returning rating forms to collect structured
    feedback from the consumer.

    '
  license:
    name: CC-BY-NC-SA 4.0 International
  contact:
    name: Beckn Labs
    url: https://beckn.io
paths: {}
components:
  schemas:
    OnRateAction:
      title: On Rate Action
      description: 'Beckn /beckn/on_rate message payload. Sent by a BPP to a BAP in

        response to a /beckn/rate call, optionally returning rating forms

        to collect structured feedback from the consumer.

        (Context wrapper stripped; only the message-content portion is inlined.)'
      type: object
      additionalProperties: false
      properties:
        ratings:
          type: array
          items:
            $ref: https://schema.beckn.io/RatingInput/v2.1/attributes.yaml#/components/schemas/RatingInput
      required:
      - ratings
