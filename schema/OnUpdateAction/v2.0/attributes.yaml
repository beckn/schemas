openapi: 3.1.1
info:
  title: OnUpdateAction
  version: 2.0.0
  description: 'Beckn /beckn/on_update callback envelope. Sent by a BPP to a BAP in
    response to a /beckn/update call (or as an unsolicited update push), returning
    the updated contract state.

    '
  license:
    name: CC-BY-NC-SA 4.0 International
  contact:
    name: Beckn Labs
    url: https://beckn.io
paths: {}
components:
  schemas:
    OnUpdateAction:
      title: On Update Action
      description: 'Beckn /beckn/on_update message payload. Sent by a BPP to a BAP
        in

        response to a /beckn/update call (or as an unsolicited update push),

        returning the updated contract state.

        (Context wrapper stripped; only the message-content portion is inlined.)'
      additionalProperties: false
      properties:
        contract:
          $ref: https://schema.beckn.io/Contract/v2.0/attributes.yaml#/components/schemas/Contract
      required:
      - contract
