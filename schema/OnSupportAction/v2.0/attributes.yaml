openapi: 3.1.1
info:
  title: OnSupportAction
  version: 2.0.0
  description: 'Beckn /beckn/on_support callback envelope. Sent by a BPP to a BAP
    in response to a /beckn/support call, returning support contact details and available
    channels.

    '
  license:
    name: CC-BY-NC-SA 4.0 International
  contact:
    name: Beckn Labs
    url: https://beckn.io
paths: {}
components:
  schemas:
    OnSupportAction:
      title: On Support Action
      description: 'Beckn /beckn/on_support message payload. Sent by a BPP to a BAP
        in

        response to a /beckn/support call, returning support contact details

        and available channels.

        (Context wrapper stripped; only the message-content portion is inlined.)'
      additionalProperties: false
      properties:
        support:
          $ref: https://schema.beckn.io/Support/v2.0/attributes.yaml#/components/schemas/Support
      required:
      - support
