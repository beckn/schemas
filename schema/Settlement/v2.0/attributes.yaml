openapi: 3.1.1
info:
  title: Settlement
  version: 2.0.0
  description: This schema is part of the Long Term Support of Beckn Protocol V2.0
    API specification and MUST NOT be extended. Any domain-specific extension must
    use the property of this schema which is of type Attribute.
paths: {}
components:
  schemas:
    Settlement:
      type: object
      properties:
        id:
          type: string
        considerationId:
          $ref: https://schema.beckn.io/Settlement/attributes.yaml#/components/schemas/Settlement
        status:
          type: string
          enum:
          - DRAFT
          - COMMITTED
          - COMPLETE
        settlementAttributes:
          $ref: https://schema.beckn.io/Attributes/attributes.yaml#/components/schemas/Attributes
      x-lts: true
      description: This schema is part of the Long Term Support of Beckn Protocol
        V2.0 API specification and MUST NOT be extended. Any domain-specific extension
        must use the property of this schema which is of type Attribute.
