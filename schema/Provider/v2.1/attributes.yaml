openapi: 3.1.1
info:
  title: Provider
  version: 2.1.0
  description: 'Schema definition for Provider in the Beckn Protocol v2.0.1


    This schema is part of the Long Term Support of Beckn Protocol V2.0 API specification and MUST NOT be extended. Any domain-specific extension must use the property of this schema which is of type Attribute.'
  license:
    name: CC-BY-NC-SA-4.0
  contact:
    name: Beckn Labs
    url: https://beckn.io
paths: {}
components:
  schemas:
    Provider:
      type: object
      description: 'Schema definition for Provider in the Beckn Protocol v2.0.1


        This schema is part of the Long Term Support of Beckn Protocol V2.0 API specification and MUST NOT be extended. Any domain-specific extension must use the property of this schema which is of type Attribute.'
      title: Provider
      properties:
        id:
          description: Unique identifier for the provider
          type: string
          example: tech-store-001
        descriptor:
          $ref: https://schema.beckn.io/Descriptor/attributes.yaml#/components/schemas/Descriptor
        availableAt:
          description: Physical locations where the provider operates
          type: array
          items:
            $ref: https://schema.beckn.io/Location/attributes.yaml#/components/schemas/Location
        providerAttributes:
          $ref: https://schema.beckn.io/Attributes/attributes.yaml#/components/schemas/Attributes
      required:
      - id
      - descriptor
      additionalProperties: false
      x-lts: true
