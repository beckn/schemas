openapi: 3.1.1
info:
  title: OnStatusAction
  version: 2.0.0
  description: 'Beckn /beckn/on_status callback envelope. Sent by a BPP to a BAP in
    response to a /beckn/status call (or as an unsolicited status push), returning
    the current state of the contract.

    '
  license:
    name: CC-BY-NC-SA 4.0 International
  contact:
    name: Beckn Labs
    url: https://beckn.io
paths: {}
components:
  schemas:
    OnStatusAction:
      title: On Status Action
      description: 'Beckn /beckn/on_status message payload. Sent by a BPP to a BAP
        in

        response to a /beckn/status call (or as an unsolicited status push),

        returning the current state of the contract.

        (Context wrapper stripped; only the message-content portion is inlined.)'
      additionalProperties: false
      properties:
        contract:
          $ref: https://schema.beckn.io/Contract/v2.0/attributes.yaml#/components/schemas/Contract
      required:
      - contract
