openapi: 3.1.1
info:
  title: Beckn OnCancelAction Schema
  description: Schema definition for the Beckn /beckn/on_cancel callback envelope.
  version: 2.0.1
  contact:
    name: Beckn Labs
    url: https://beckn.io
  license:
    name: CC-BY-NC-SA 4.0 International
    url: https://creativecommons.org/licenses/by-nc-sa/4.0/deed.en
components:
  schemas:
    OnCancelAction:
      description: >
        Beckn /beckn/on_cancel callback envelope. Sent by a BPP to a BAP in
        response to a /beckn/cancel call, returning the contract with status
        set to CANCELLED and any applicable cancellation outcome.
      type: object
      properties:
        context:
          allOf:
            - $ref: 'https://schema.beckn.io/Context.yaml'
            - type: object
              properties:
                action:
                  type: string
                  const: beckn/on_cancel
        message:
          type: object
          additionalProperties: true
          properties:
            contract:
              allOf:
                - $ref: 'https://schema.beckn.io/Contract.yaml'
          required:
            - contract
      required:
        - context
        - message
