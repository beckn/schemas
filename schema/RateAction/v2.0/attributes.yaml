openapi: 3.1.1
info:
  title: RateAction
  version: 2.0.0
  description: 'Beckn /beckn/rate action envelope. Sent by a BAP to a BPP to submit
    one or more rating inputs for entities in a completed contract (order, fulfillment,
    item, provider, agent, support).

    '
  license:
    name: CC-BY-NC-SA 4.0 International
  contact:
    name: Beckn Labs
    url: https://beckn.io
paths: {}
components:
  schemas:
    RateAction:
      title: RateAction
      description: 'Beckn /beckn/rate message payload. Sent by a BAP to a BPP to submit

        one or more rating inputs for entities in a completed contract

        (order, fulfillment, item, provider, agent, support).

        (Context wrapper stripped; only the message-content portion is inlined.)'
      type: object
      additionalProperties: false
      properties:
        ratingInputs:
          type: array
          items:
            $ref: https://schema.beckn.io/RatingInput/v2.1/attributes.yaml#/components/schemas/RatingInput
      required:
      - ratingInputs
