openapi: 3.1.1
info:
  title: OnConfirmAction
  version: 2.0.0
  description: 'Beckn /beckn/on_confirm callback envelope. Sent by a BPP to a BAP
    in response to a /beckn/confirm call, returning the confirmed contract with status
    set to CONFIRMED.

    '
  license:
    name: CC-BY-NC-SA 4.0 International
  contact:
    name: Beckn Labs
    url: https://beckn.io
paths: {}
components:
  schemas:
    OnConfirmAction:
      title: OnConfirmAction
      description: 'Beckn /beckn/on_confirm message payload. Sent by a BPP to a BAP
        in

        response to a /beckn/confirm call, returning the confirmed contract

        with status set to CONFIRMED.

        (Context wrapper stripped; only the message-content portion is inlined.)'
      additionalProperties: false
      properties:
        contract:
          $ref: https://schema.beckn.io/Contract/v2.0/attributes.yaml#/components/schemas/Contract
      required:
      - contract
