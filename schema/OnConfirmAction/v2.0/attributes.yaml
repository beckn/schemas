openapi: 3.1.1
info:
  title: Beckn OnConfirmAction Schema
  description: Schema definition for the Beckn /beckn/on_confirm callback envelope.
  version: 2.0.1
  contact:
    name: Beckn Labs
    url: https://beckn.io
  license:
    name: CC-BY-NC-SA 4.0 International
    url: https://creativecommons.org/licenses/by-nc-sa/4.0/deed.en
components:
  schemas:
    OnConfirmAction:
      description: >
        Beckn /beckn/on_confirm callback envelope. Sent by a BPP to a BAP in
        response to a /beckn/confirm call, returning the confirmed contract with
        status set to CONFIRMED.
      type: object
      properties:
        context:
          allOf:
            - $ref: 'https://schema.beckn.io/Context.yaml'
            - type: object
              properties:
                action:
                  type: string
                  const: beckn/on_confirm
        message:
          type: object
          additionalProperties: true
          properties:
            contract:
              allOf:
                - $ref: 'https://schema.beckn.io/Contract.yaml'
          required:
            - contract
      required:
        - context
        - message
