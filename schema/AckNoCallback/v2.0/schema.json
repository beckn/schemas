{
  "description": "Request received but no callback will follow (e.g. agents not found, inventory unavailable, provider closed, context.try preview complete).\n\nThis schema is part of the Long Term Support of Beckn Protocol V2.0 API specification and MUST NOT be extended. Any domain-specific extension must use the property of this schema which is of type Attribute.",
  "allOf": [
    {
      "$ref": "https://schema.beckn.io/Ack/attributes.yaml"
    },
    {
      "type": "object",
      "properties": {
        "error": {
          "$ref": "https://schema.beckn.io/Error/attributes.yaml"
        }
      }
    },
    {
      "required": [
        "status",
        "signature",
        "error"
      ]
    }
  ],
  "x-lts": true,
  "$id": "https://schema.beckn.io/AckNoCallback/v2.0",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "AckNoCallback"
}
