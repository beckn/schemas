openapi: 3.1.1
info:
  title: UpdateAction
  version: 2.0.0
  description: 'Beckn /beckn/update action envelope. Sent by a BAP to a BPP to request
    changes to an active contract (e.g., update fulfillment address, add items, change
    quantities). The context.try flag must be true during negotiation.

    '
  license:
    name: CC-BY-NC-SA 4.0 International
  contact:
    name: Beckn Labs
    url: https://beckn.io
paths: {}
components:
  schemas:
    UpdateAction:
      title: Update Action
      description: 'Beckn /beckn/update message payload. Sent by a BAP to a BPP to
        request

        changes to an active contract (e.g., update fulfillment address, add items,

        change quantities). The context.try flag must be true during negotiation.

        (Context wrapper stripped; only the message-content portion is inlined.)'
      additionalProperties: false
      properties:
        contract:
          $ref: https://schema.beckn.io/Contract/v2.0/attributes.yaml#/components/schemas/Contract
      required:
      - contract
