openapi: 3.1.1
info:
  title: AsyncError
  version: 2.0.0
  description: 'Error returned asynchronously during a callback. Wraps the base `Error`
    schema with JSON-LD type annotations to allow linked-data processing.


    This schema is part of the Long Term Support of Beckn Protocol V2.0 API specification
    and MUST NOT be extended. Any domain-specific extension must use the property
    of this schema which is of type Attribute.'
  license:
    name: CC-BY-NC-SA 4.0 International
  contact:
    name: Beckn Labs
    url: https://beckn.io
paths: {}
components:
  schemas:
    AsyncError:
      title: Asynchronous Error
      description: Error returned asynchronously during a callback. Wraps the base
        `Error` schema with JSON-LD type annotations to allow linked-data processing.
      allOf:
      - $ref: https://schema.beckn.io/Error/v2.0/attributes.yaml#/components/schemas/Error
      - type: object
        properties: {}
        additionalProperties: true
