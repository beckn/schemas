openapi: 3.1.1
info:
  title: ServerError
  version: 2.0.0
  description: 'Internal failure on the network participant''s application; the request
    could not be processed. The response body MAY contain an `error` object with additional
    details.


    This schema is part of the Long Term Support of Beckn Protocol V2.0 API specification
    and MUST NOT be extended. Any domain-specific extension must use the property
    of this schema which is of type Attribute.'
  license:
    name: CC-BY-NC-SA 4.0 International
  contact:
    name: Beckn Labs
    url: https://beckn.io
paths: {}
components:
  schemas:
    ServerError:
      description: Internal failure on the network participant's application; the
        request could not be processed. The response body MAY contain an `error` object
        with additional details.
      type: object
      properties:
        error:
          $ref: https://schema.beckn.io/Error/v2.0/attributes.yaml#/components/schemas/Error
