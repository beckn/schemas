{
  "description": "Catalog publish processing results from CDS to BPP.",
  "type": "object",
  "required": [
    "results"
  ],
  "properties": {
    "results": {
      "type": "array",
      "description": "Per-catalog processing results",
      "items": {
        "$ref": "https://schema.beckn.io/CatalogProcessingResult/v2.0/schema.json"
      }
    }
  }
}
