openapi: 3.1.1
info:
  title: CatalogOnPublishAction
  version: 2.0.0
  description: 'Catalog publish processing results from CDS to BPP.


    This schema is part of the Long Term Support of Beckn Protocol V2.0 API specification
    and MUST NOT be extended. Any domain-specific extension must use the property
    of this schema which is of type Attribute.'
paths: {}
components:
  schemas:
    CatalogOnPublishAction:
      description: 'Catalog publish processing results from CDS to BPP.


        This schema is part of the Long Term Support of Beckn Protocol V2.0 API specification
        and MUST NOT be extended. Any domain-specific extension must use the property
        of this schema which is of type Attribute.'
      type: object
      required:
      - results
      properties:
        results:
          type: array
          description: Per-catalog processing results
          items:
            $ref: https://schema.beckn.io/CatalogProcessingResult/attributes.yaml#/components/schemas/CatalogProcessingResult
      x-lts: true
