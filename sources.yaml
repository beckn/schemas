# sources.yaml
# Schema source registry for schema.beckn.io.
#
# Each entry declares a GitHub-hosted schema source:
#   schemaPath is always a GitHub tree URL:
#     https://github.com/<owner>/<repo>/tree/<ref>/<path>
#
# Fields:
#   schemaPath  : GitHub tree URL pointing to the schema folder.
#   description : Human-readable summary.
#   tags : Domain tags applied to all schemas sourced from the schema repo
sources:

  # ── Beckn Core Schema Registry ───────────────────────────────────────────────
  # This repo. Contains core protocol schemas plus domain schemas for mobility,
  # logistics, and retail. All schemas carry x-tags per domain.
  - schemaPath: https://github.com/beckn/core_schema/tree/draft-preview/schema
    description: >-
      Beckn Core Schema Registry. Contains core protocol schemas plus domain schemas
      for mobility, logistics, and retail. Schemas are tagged per-domain via x-tags.
    tags:
      - core

  # ── Digital Energy Grid ───────────────────────────────────────────────────────
  # DEG maintains its own schemas in their GitHub repo. schema.beckn.io indexes
  # them via this entry. 
  - schemaPath: https://github.com/beckn/DEG/tree/draft-preview/specification/schema
    description: >-
      Digital Energy Grid domain schemas for P2P energy trading, EV charging,
      and related energy use cases.
    tags:
      - energy
